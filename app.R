#
# Deze shiny app maakt het mogelijk om zelf een score bestand te uploaden
# vragen te selecteren voor analyse en op basis daarvan een itemanalyse
# te maken
#

# Run the application 
shinyApp(ui = ui, server = server)
